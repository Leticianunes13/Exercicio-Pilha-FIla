# Verificador de Palíndromos

Este projeto é um exercício prático de Estruturas de Dados desenvolvido em Python. O sistema verifica se uma palavra ou frase é um palíndromo (lê-se igual de trás para frente) utilizando as estruturas de dados **Pilha (Stack)** e **Fila (Queue)**.

## 🚀 Funcionalidades
* Ignora espaços, pontuações e acentos na validação.
* Não faz distinção entre letras maiúsculas e minúsculas (*case-insensitive*).
* Fila (Queue) implementada do zero usando **Listas Encadeadas** (Nós/Ponteiros).
* Pilha (Stack) implementada usando **Vetores** (Listas nativas do Python).

## ⚙️ Como Executar
1. Salve o código em um arquivo `.py` (ex: `palindromo.py`).
2. Abra o terminal na mesma pasta do arquivo e rode o comando:
   ```bash
   python palindromo.py
